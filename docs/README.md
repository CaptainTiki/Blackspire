# Blackspire

## North Star

**Blackspire is a four-player procedural dungeon raid about discovery, greed, survival, and escape.** It recreates the feeling of early MMO raiding before every answer was known: your team enters an unfamiliar dungeon, figures out its routes and puzzles under pressure, gathers as much loot as you dare, and must survive the return trip to keep it.

---

v0.0.0028 : Generic drop-from-backpack completed and smoke-verified. Added `world_pickup_scene_path` to `ItemDefinition`, so every item definition can point to the scene it should spawn when dropped without hardcoded item mappings or cyclic scene preloads. Existing definitions now point to their pickup scenes: gold, rusted sword, padded vest, and worn pack. Converted pickup roots from `StaticBody3D` to `RigidBody3D`, preserving their interactable pickup behavior while allowing dropped items to be tossed into the world. Added a Drop button to the paper-doll backpack panel. Dropping is backpack-only for this slice: hold an inventory item, click Drop, and the item leaves the backpack, clears any hotbar binding to that item instance, instantiates its pickup scene near the player camera, configures it from the exact `ItemInstance`, and applies a small forward/up impulse. Gold drops preserve stack quantity through `coin_amount`; equipment drops preserve their equipment definition. Verified with Godot check-only, a focused drop smoke script covering sword drop, rigid-body pickup creation, hotbar binding cleanup, gold stack quantity preservation, and inventory removal, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0027 : Hotbar binding foundation completed and smoke-verified. Added `HotbarBinding` as a lightweight resource that links a hotbar slot to a real backpack `ItemInstance` instead of moving or copying the item. Added `PlayerHotbar` as a player-owned component with dynamic slot count from `PlayerInventory.get_hotbar_slot_count()`, binding storage, placeholder activation, and hotbar toasts. The player scene now owns `PlayerHotbar`, and `PlayerEquipmentUI` can bind held backpack items to visible hotbar slots: the held item is returned to its backpack slot, the hotbar stores a link to that same item instance, and the UI shows the linked item name. Moving the item between backpack slots preserves the binding because the binding points at the instance, not a slot index. Hotbar slot activation through click or number keys 1-4 currently produces placeholder feedback such as equipment/consumable use not implemented. Bag capacity still expands visible hotbar slots. Verified with Godot check-only, a focused hotbar binding smoke script covering baseline slot count, backpack-to-hotbar binding, item staying in backpack, binding surviving backpack movement, placeholder activation feedback, and worn-pack hotbar expansion, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0026 : Mouse equip-from-backpack completed and smoke-verified. Added `inventory_pick_place` and `inventory_cancel_drag` mouse actions to the project input map. Backpack slots now preserve explicit slot positions, including empty holes, so clicked items can move and swap predictably. The paper-doll UI now supports mouse-first item handling: click a backpack item to hold it, click an empty backpack slot to place it, click an occupied backpack slot to swap, click an empty compatible equipment slot to equip, and click an occupied equipment slot to hold/unequip that item. Equipment-slot swaps return the replaced equipped item to the cursor-held item. The held item follows the mouse as a small cursor label. Capacity-reducing equipment changes are blocked if removing/replacing the item would leave too many backpack slots occupied. Equipment pickups now add their equipment item to the backpack instead of direct-equipping, so the loot loop is pickup -> backpack -> paper-doll equip. Hotbar slots remain visible but nonfunctional; they intentionally do not accept item movement yet because hotbar assignment should be a link to an inventory item rather than moving the item itself. Verified with Godot check-only, a focused mouse inventory smoke script covering backpack hold/place, equipment equip/unequip, occupied-slot swap, bag equip capacity growth, and equipment pickup-to-backpack behavior, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0025 : Backpack capacity enforcement completed and smoke-verified. `PlayerInventory.add_item()` and `add_coins()` now return success/failure and enforce backpack capacity before mutating inventory. Capacity is currently all-or-nothing: stackable items can use open space in matching stacks plus empty slots, non-stackable items require one empty slot per instance, and rejected pickups do not partially collect. `Pickup.collect()` now only marks itself collected and queues free when the inventory accepts the item; otherwise the pickup remains in the world and the player sees an inventory toast such as "Not enough backpack space for gold." The paper-doll stats readout now shows backpack usage as used/total slots. Verified with Godot check-only, a focused inventory capacity smoke script covering full baseline backpacks, failed item/gold pickup rejection, pickup persistence, worn-pack capacity expansion, and post-bag pickup success, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0024 : Bag equipment capacity pipeline completed and smoke-verified. Added `BAG` as an equipment slot and added `BACKPACK_SLOTS` / `HOTBAR_SLOTS` stat modifier types. Added a first `worn_pack` equipment definition that grants +3 backpack slots and +1 hotbar slot, plus a placeable `worn_pack_pickup` scene exposed through the TrenchBroom FGD as `pickup_worn_pack`. Placed the worn pack in the treasure room alongside the sword and vest. `PlayerInventory` now computes backpack and hotbar counts from its base values plus equipped item modifiers, and `PlayerEquipmentUI` rebuilds the backpack/hotbar buttons when those counts change. The stats panel also shows current backpack and hotbar counts, so the pickup path has an immediate smoke-test readout. Verified with Godot check-only, a focused bag capacity smoke script covering 5/3 baseline counts, 8/4 worn-pack counts, UI rebuilds, and treasure-room placement, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0023 : Backpack and hotbar UI foundation completed and smoke-verified. Added baseline inventory-space metadata to `PlayerInventory`: players currently start with 5 backpack slots and a 3-slot placeholder hotbar. The Tab paper-doll panel now includes a Backpack section populated from `PlayerInventory.get_items()` and a visible nonfunctional Hotbar section for future potion/item usage. Backpack slot focus updates the existing item-detail panel with quantity and item stats when available, while equipment focus still shows equipped-slot details. Updated the GDD with the intended bag direction: bags remain future equipment that can expand backpack capacity and alter hotbar size, with split/drop/drag/use intentionally deferred. Verified with Godot check-only, a focused backpack UI smoke script covering slot counts, hotbar placeholders, gold stack display, and backpack detail focus, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0022 : Item backend foundation completed and smoke-verified. Added `ItemInstance` as the runtime item model that references an `ItemDefinition`, tracks quantity, supports stack checks, stack-space queries, quantity adding, splitting, and unique instance ids for non-stackables. Upgraded `PlayerInventory` from a coins-only counter into a small item-instance backend with `add_item()`, stack merging, max-stack overflow, `get_items()`, `get_item_count()`, and `has_item()`. Gold still works through `add_coins()` and extraction summaries, but now uses the `gold_coin` item definition and item stacks internally. Inventory pickups emit a player-local toast/readout through `inventory_toast`, currently displayed by `PlayerEquipmentUI`'s existing feedback label, so backend smoke remains visible without adding the inventory grid yet. Verified with Godot check-only, a focused inventory backend smoke script covering gold stack creation, max-stack overflow, coin count, exit summary, non-stackable item instances, splitting, and toast text, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0021 : Armor mitigation and stat pipeline hardening completed and smoke-verified. Added named stat accessors to `PlayerEquipment` for attack damage, armor, max health, and move speed so gameplay and UI stop reaching through raw stat IDs everywhere. Added `PlayerEquipment.mitigate_physical_damage()`, currently using flat armor reduction with at least 1 chip damage for positive incoming physical hits. `PlayerController` now owns the player-side damage entry point: physical `DamageRequest`s are mitigated by equipped armor before reaching `HealthComponent`, while non-physical damage bypasses armor. Enemy attacks now call `target.apply_damage(damage_request)` instead of reaching directly into `Components/HealthComponent`, keeping actor-specific mitigation possible and multiplayer-shaped. Added `DamageRequest.with_amount()` for preserving source, hit position, and damage type when damage amounts are adjusted. Verified with Godot check-only, a focused armor mitigation smoke script covering unarmored damage, padded vest mitigation, chip damage, non-physical bypass, and request copying, plus `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0020 : Paper-doll + Stats completed and smoke-verified. Added a player-owned `PlayerEquipmentUI` CanvasLayer toggled by the new `toggle_equipment` input action on Tab. The panel is intentionally an equipment inspection screen, not a full inventory: it shows equipment slots, the focused slot/item details, and a small current stats panel for attack damage, armor, max health modifier, and move speed modifier. `PlayerEquipment` changes now drive a short "Equipped X" feedback toast, so direct equip-on-pickup has visible confirmation. The UI reads directly from that player's `PlayerEquipment` and `PlayerMeleeAttack`, keeping the shape multiplayer-friendly. Verified with Godot check-only, a focused equipment UI smoke script that equipped the rusted sword and padded vest, opened the panel, and checked slot/detail/stat text, and `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0019 : Direct Equip-on-Pickup completed and smoke-verified. Added `PlayerEquipment` as a tiny player-owned equipment component that tracks equipped definitions by slot and totals stat modifiers across equipped gear. Added `EquipmentPickup`, an interactable pickup scene base backed by an `EquipmentDefinition`, then added placeable `rusted_sword_pickup.tscn` and `padded_vest_pickup.tscn` world scenes. Added TrenchBroom point entities `pickup_rusted_sword` and `pickup_padded_vest` to the Blackspire FGD so the new equipment can be sprinkled into authored maps, then placed one of each in `room_treasure_01`. `PlayerMeleeAttack` now asks equipped gear for `ATTACK_DAMAGE` modifiers, so the rusted sword raises prototype melee damage from 10 to 14. Verified with Godot check-only, focused pickup and treasure-room placement smoke scripts, and `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0018 : Equipment Definition Foundation completed and smoke-verified. Added enum-backed `ItemDefinition`, `EquipmentDefinition`, and `StatModifierDefinition` resources under `data/items`, keeping the new item layer pure data for now. `ItemDefinition` owns mapper/designer-facing item identity, display name, icon, stackability, max stack, and item type. `EquipmentDefinition` extends item data with an equipment slot and an array of stat modifier resources, allowing hand-authored positive and negative modifiers without building inventory UI yet. Added sample definitions for `gold_coin`, `rusted_sword`, and `padded_vest`; the vest demonstrates both positive armor and negative move-speed modifiers. Verified with Godot check-only, a focused resource smoke script that loaded all three sample definitions and checked modifier totals, and `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0017 : Small player HP HUD completed and smoke-verified. Added a player-owned `PlayerHUD` CanvasLayer under the player scene, bound directly to that player's `HealthComponent`, showing current/max HP in the lower-left overlay. Added `HealthComponent.health_changed(current_health, max_health)` so HUD code can listen to health changes cleanly instead of inferring from damage/death only. HUD updates on damage and revive. Verified with a focused smoke script that instantiated the player, checked `HP 100/100`, applied damage to update to `HP 83/100`, then revived to `HP 42/100`; remaining headless output is the known cleanup-warning noise.

v0.0.0016 : Player bleedout and run-fail slice completed and smoke-verified. Added `PlayerLifeState` as the player-specific bridge from generic `HealthComponent.died` into multiplayer-shaped run lifecycle: at zero HP the player enters bleeding out, controls/interactions/attacks are locked, the camera collapses toward the floor, and the player notifies `Level`. Bleeding out lasts 60 seconds by default, then marks the player dead and notifies `Level` again. Added a minimal revive hook for later work without wiring revive gameplay yet. `Level` now registers active players, emits player bleeding/death signals, and shows a run-failed overlay when every active player is bleeding out or dead, while keeping extraction success separate. Updated player interaction and melee attack components so downed players cannot interact or swing. Verified with a two-player smoke script: Player A goes down without failing the run while Player B is active, Player A dies after bleed-out, then Player B going down fails the run; remaining headless output is the known cleanup-warning noise.

v0.0.0015 : Extraction confirmation pass completed and smoke-verified. Refined delayed extraction so countdown completion no longer auto-exits the run. `Level.begin_extraction()` now charges the portal, alerts active enemies, and marks `is_extraction_ready` when the timer reaches zero. The countdown overlay changes from `Extraction in N` to `Extraction Ready`; the player must interact with the `ExitPortal` again to complete through `Level.exit_level(player)`. This keeps extraction from becoming a remote delayed escape after the player starts the timer and runs away. Verified with a focused smoke script that starts extraction, advances the countdown, confirms the run is not completed at readiness, verifies the overlay text, then interacts again to close the run; remaining headless output is the known cleanup-warning noise.

v0.0.0014 : Delayed extraction pressure pass completed and smoke-verified. Changed extraction from a hard "kill every spawned enemy" gate into a 15-second extraction countdown: `ExitPortal` now calls `Level.begin_extraction(player, extraction_delay_seconds)` instead of directly exiting or blocking on living enemies. `Level` tracks extraction-in-progress state, displays a temporary countdown overlay, and completes through `Level.exit_level(player)` when the timer reaches zero. Starting extraction now alerts every active spawned enemy to the extracting player through `BasicEnemy.alert_to_player()` and `EnemyBehavior.alert_to_target()`, forcing enemies into chase even if they were outside normal aggro range. Updated the TrenchBroom `exit_portal` FGD property from `requires_all_enemies_defeated` to `extraction_delay_seconds`, and updated the exit room map to author the current 15-second prototype delay. Verified with Godot check-only and a focused delayed-extraction smoke script that starts extraction while enemies remain, confirms enemies target the player, and completes after the countdown; remaining headless output is the known cleanup-warning noise.

v0.0.0013 : First loot pickup slice completed and smoke-verified. Added a tiny player-owned `PlayerInventory` component that tracks only collected coins, keeping loot state off `Level` for the future multiplayer shape. Added an interactable `Pickup` base entity plus `GoldPickup`, exposed through the TrenchBroom `pickup_gold` point entity and exported FGD, so mapper-authored loot uses the same interaction pattern as doors, levers, urns, and portals. Updated the treasure room map/generated scene with three gold pickups worth 25 gold total. Updated extraction so `ExitPortal` calls `Level.exit_level(player)`: the level gates extraction through living enemies, emits exit/completion signals, and displays the player's loot summary without owning inventory itself. Kept `Level.complete_run()` as a compatibility wrapper that now delegates to `exit_level(player)`. Verified with targeted loot/extraction smoke coverage, Godot check-only, short headless boot, and `git diff --check`; remaining headless output is the known cleanup-warning noise.

v0.0.0012 : Prototype escape loop milestone completed and smoke-verified. Added a TrenchBroom-authored `exit_portal` point entity backed by `ExitPortal`, with mapper-facing prompt, completion message, and `requires_all_enemies_defeated` properties in both the Godot FGD resource and exported `Blackspire.fgd`. Added `room_exit_01.map` and generated `room_exit_01.tscn`, using the existing crypt room materials and a visible portal plinth/glow/label. Extended the authored `test_level` chain so the elite room connects through another 64-unit hallway into the exit room, keeping the current rule that semantic rooms do not connect directly. Added `Level.has_living_enemies()` and `Level.complete_run()`: the portal blocks extraction while spawned enemies remain, then emits `run_completed`, prints the completion state, shows a temporary run-complete overlay, and pauses the tree. Verified with a targeted exit-loop smoke script, Godot check-only, and headless boot; remaining headless output is the known cleanup-warning noise.

v0.0.0011 : Elite enemy milestone completed and play-verified. Added `EnemySpawnMarker` as the scene-backed TrenchBroom `info_enemy_spawn` script with an `elite` Yes/No mapper property, exported the updated `Blackspire.fgd`, and pointed the TrenchBroom game config at it so authored enemy markers can choose basic or elite spawns. Added `elite_enemy.tscn` as an inherited heavier prototype enemy: larger scale, 120 health, slower movement, longer attack range, wider/harder forward cone lunge, and stronger hit feedback while still reusing the current basic enemy brain. Updated the final elite room with one elite spawn marker, and updated `Level.spawn_enemies()` to choose the basic or elite scene while preserving authored room/spawn transforms. Enemy cleanup now has clearer lifecycle naming: `die()` starts the death process, a short `DeathTimer` gives us a future hook for particles/animation/fade, and `decompose()` removes the enemy from `Level.spawned_enemies` before `queue_free()`. Verified with elite spawn/decompose smoke checks, Godot check-only, and headless boot; remaining headless output is the known cleanup-warning noise.

v0.0.0010 : Manual room-chain generation milestone completed and play-verified. Added reusable `Room` metadata, TrenchBroom-authored `room_connector` point entities, `RoomChainEntry`, and `LevelGenerator` so authored room scenes can be assembled by aligning connector origins and opposing connector outward directions. Added procedural first-pass crypt floor/wall/trim textures and authored TrenchBroom maps for spawn, 64-unit hallway, 4-way combat, treasure, small combat, and larger elite-room shell. Updated `test_level` to generate a corridor-separated dungeon chain with a treasure side branch: spawn -> hallway -> 4-way combat, plus hallway -> treasure and hallway -> small combat -> hallway -> elite room. Verified in play that the player spawns in the generated spawn room, traversal across hallway-separated room joins works, enemies spawn in generated combat rooms, urns appear in the generated treasure room, and the larger final room is in place for the next elite-enemy iteration. Godot check/headless smoke passes completed; remaining headless output is the known cleanup-warning noise.

v0.0.0009 : Enemy readability and combat feel pass completed and play-verified. `BasicEnemy` now has a visible windup tell, squash/lean anticipation, committed forward hop-lunge, 115% attack pop, hit reaction, debug state label, timed world health bar, and visible attack-range debug disc for tuning. Enemy damage now resolves through a forward cone during the lunge instead of a circular end-range check, making backpedaling risky while allowing side dodges and flank punish windows. `WorldHealthBar3D` was simplified back into a pure health view: it appears after damage, stays visible briefly, then fades slowly without depending on `Interactable` focus. Added named 3D physics layers (`World`, `Interactable`, `CombatHurtbox`, `WeaponHitbox`, `Player`, `Enemy`) and wired initial masks so the interaction ray no longer sees the sword hitbox, while sword attacks target combat hurtboxes explicitly. Verified with Godot check/headless smoke passes and live feel testing.

v0.0.0008 : First combat slice completed and play-verified. Added reusable `HealthComponent`, `DamageRequest`, `Hurtbox3D`, first-person sword attack, floating damage numbers, world health bars, and player hit flash feedback. Wooden doors are now breakable by sword hits; iron doors are unbreakable but can still be interaction/lever controlled. Urns can be smashed by sword hurtbox overlap. Added `BasicEnemy` with a temporary dedicated `EnemyBehavior` enum brain (`IDLE`, `CHASE`, `ATTACK`, `DEAD`), health, hurtbox, damage feedback, death handling, and TrenchBroom entity `enemy_basic`. `Level` now consumes `info_enemy_spawn` markers and spawns basic enemies from the authored map. Verified: enemy spawns from marker, chases/attacks player, player can see hit feedback, sword damages/kills enemy, sword breaks doors/urns, and lever/door regressions still pass.

v0.0.0007 : Lever-operated door milestone completed and runtime-verified. `BaseLevel` renamed to `Level`, with a single active dungeon context exposed through `Level.current_level`. `Level` now owns an explicit `MapEntityRegistry` child used by TrenchBroom-authored `targetname` / `targets` activation. Door mapper property is now `player_operated`: false blocks direct player operation while still allowing external activators. Lever scene now has collider + placeholder visuals, receives FuncGodot properties via `@tool` scripts, and toggles target doors open/closed. Verified: player cannot operate remote-only doors, lever opens/closes target door, collision blocks while closed, and player can pass while open.

v0.0.0006 : Swinging door system completed and runtime-verified. `SwingDoor` base now delivers consistent push-to-open behavior from both sides using facing-based detection + stored open side for closing. Four animations (open_a/b + close_a/b). Proper `Interactable` wired. Locked door support + explicit testing path. `MapEntityRegistry` introduced for clean targetname-based lever/door (and future) connections. Basic Lever entity with `is_active`/`has_been_used` state machine. Strong "no fallbacks / fail loudly" + short focused scripts philosophy reinforced across the codebase. Lightweight diagnostics continue to be very effective.

v0.0.0005 : Completed Breakable Urn as a working interactable. Debris pieces now own their lifetime via Timer + "shrink" AnimationPlayer, then `queue_free()`. Fully functional using the self.owner pattern (no longer dependent on Room `initialize()`). Continued refinement of lightweight diagnostic testing approach.

v0.0.0004 : Entity self-discovery via `self.owner` (replacing Room-pushed `initialize()` for the Breakable Urn). Removed `initialize()` method from base `Entity` + `BreakableUrnEntity`, and removed the initialization loop from `Room` for urns. Established lightweight goal-oriented diagnostic testing process (`.tests/` directory + headless runs with explicit goal scorecards). Continued Breakable Urn work (thin `Interactable` component + dedicated `BreakableUrnEntity` root). Moved urn to `world/entities/urn/`.

v0.0.0003 : Interaction system design + component architecture. Room initialization pattern (`initialize()`). Player height locked at 1.45 / 1.30. 64-unit standard room height adopted. Crypt biome selected as first test level. Started `Room` base class and `InteractionScanner` / `Interactable` components. First interactable target: breakable urn.

v0.0.0002 : project refinement

v0.0.0001 : project setup
